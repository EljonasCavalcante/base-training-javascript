function areaQuadrada(lado) {
    console.log('função ativada');
    return lado * lado;
}
console.log(areaQuadrada(10))

//=====================================================
function pi() {
    return 3.14;
  }
  
  var total = 5 * pi(); // 15.7
  console.log(pi(),` resposta é ${total}`)

  function imc (peso, altura) {
      imc = peso / (altura * altura)
      return imc
  }

  console.log(imc(80, 1.80))




